import { ReferencesGrid } from "../ReferencesGrid";

export const References = () => (
  <section
    id="references"
    className="min-h-screen flex flex-col items-center justify-center w-full pt-12"
  >
    <h2 className="text-white text-5xl font-bold mb-3 mt-5">References</h2>

    <ReferencesGrid />
  </section>
);
